#include "Data.h"

#include <iostream>
#include <fstream>


Data::Data(std::string fileName)
{
   /*
   std::ifstream infile;
   infile.open(fileName);
   std::string STRING;
   while (!infile.eof)
   {
      std::getline(infile, STRING);
      std::cout << STRING << std::endl;
   }
   infile.close();
   system("pause");
   */
}


Data::~Data()
{
}
